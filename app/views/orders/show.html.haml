- if @order.order_items.empty?
  Add 10 sessions to #{@order.contract.client.name}'s account for #{@user.pricebook_entry.unitprice} a session?
  / make form param to add in unit price
  = link_to 'Add 10 Credits', order_path(@order), method: :put
- else
  %h4 Order Total
  Bring us the girl and wipeaway the debt.
  / SF calculated fields can take up to 10 min to sync back down, unless on streaming api
  / likely won't want to depend on those fields for immediate view presentation
  = number_to_currency (@order.totalamount || @order.order_items.last.unitprice * @order.order_items.last.quantity)
  = link_to 'Add Session', events_path(order: @order.sfid), method: :post # new_event_path(order: @order.sfid)
  %h3 Events
  - @order.events.each do |event|
    %h5 #{event.subject}
    = event.starts
    = event.durationinminutes 
    Minutes
